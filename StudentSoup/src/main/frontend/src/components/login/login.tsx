import LoginNavbar from '../common/loginNavbar';
import LoginForm from './loginform';

const Login = () => {
  return (
    <div>
      <LoginNavbar />
      <LoginForm />
    </div>
  );
};

export default Login;
